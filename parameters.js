//function expression(expressão)
//function anonymous (anonima)

// parâmetros (parameters)
const sum = function(number1,number2) {
    console.log(number1 + number2) //somando os dois parametros passado na função
}

sum(2,3) //arguments